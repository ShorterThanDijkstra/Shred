pub struct User {
    pub id: u64,
    pub name: String,
    pub email: String,
    passwd: String,
    pub is_valid: bool,
}

impl User {
    // pub fn register(name:String, passwd:String) -> Self {
    //
    // }
}